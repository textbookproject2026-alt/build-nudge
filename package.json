{
  "name": "build-nudge",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "check": "wrangler deploy --dry-run --outdir .dry-run"
  },
  "devDependencies": {
    "wrangler": "4.137.0"
  }
}
